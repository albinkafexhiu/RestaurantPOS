@{
    string ctrl = (ViewContext.RouteData.Values["controller"]?.ToString() ?? "").ToLower();
    Func<string, string> active = (name) => ctrl == name.ToLower() ? "active" : "";
}

<ul class="navbar-nav pt-lg-3">
    <li class="nav-item @active("pos")">
        <a class="nav-link" asp-controller="Pos" asp-action="Index">
            <span class="nav-link-icon d-md-none d-lg-inline-block">
                <i class="ti ti-layout-grid"></i>
            </span>
            <span class="nav-link-title">Tables</span>
        </a>
    </li>

    <!-- POS Expenses page: later we’ll make a dedicated PosExpensesController or reuse Expenses with POS layout -->
    <li class="nav-item @active("posexpenses")">
        <a class="nav-link" asp-controller="PosExpenses" asp-action="Index">
            <span class="nav-link-icon d-md-none d-lg-inline-block">
                <i class="ti ti-receipt"></i>
            </span>
            <span class="nav-link-title">Expenses</span>
        </a>
    </li>

    <li class="nav-item">
        <a class="nav-link" asp-controller="Auth" asp-action="Logout">
            <span class="nav-link-icon d-md-none d-lg-inline-block">
                <i class="ti ti-logout"></i>
            </span>
            <span class="nav-link-title">Logout</span>
        </a>
    </li>
</ul>